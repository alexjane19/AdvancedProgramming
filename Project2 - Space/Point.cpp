#include "Point.h"


Point::Point(LongNumber a = 0, LongNumber b = 0, LongNumber c = 0){ x = a; y = b; z = c; }

//double Point::distance(Point p1)
//{
//	//return sqrt(pow(x - p1.x, 2) + pow(y - p1.y, 2) + pow(z - p1.z, 2));
//}


Point::~Point()
{
}
